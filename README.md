# 六爻玄機引擎 v1.3.0 — 100% 純前端

零後端、零資料庫、零第三方 runtime 依賴。靜態檔即可部署 GitHub Pages。

## 已完成
- 六爻 6/7/8/9 → 本卦／動爻／變卦
- 64 卦、八宮、宮五行、世應
- 納甲天干地支、爻五行
- 六親、六神
- Gregorian → 日干支離線計算；手動日柱覆寫
- 太陽視黃經計算節氣交界與月建（Asia/Taipei，1901–2099）；手動月建覆寫
- 旬空
- 生剋、六沖、六合、刑害破資料
- 旺衰工程化可配置分數＋標籤（月破、日沖、空亡、暗動候選）
- 問事分類／手動用神
- 用神／原神／忌神／仇神角色
- 伏神／飛神：明爻缺六親時由本宮首卦同爻位裝伏神，計算飛伏生剋並納入用神
- 動爻、變爻、回頭生／回頭剋
- 可解釋綜合判斷
- 應期地支觸發條件＋最近日期候選
- localStorage 最近 30 卦
- 複製摘要、匯出單一案例 JSON、全案例 JSON／CSV
- 規則包版本、公開來源索引與可驗證範圍說明
- 事後回測：實際結果、準確度、解讀幫助度、日期與評語
- 手機版 UI
- Node 單元、整合、4096 組起卦窮舉與靜態站測試
- GitHub Pages workflow（測試通過才部署）

## 本機啟動
瀏覽器不能直接用 `file://` fetch JSON，請開靜態 HTTP server：

```bash
# 需要 Node.js 18+
npm run serve
# 開 http://127.0.0.1:8080
```

## 測試
```bash
npm test
```

## GitHub Pages
1. 建立 repository，把本資料夾內容 push 到 `main`。
2. Repository → Settings → Pages → Source 選 `GitHub Actions`。
3. `.github/workflows/pages.yml` 會部署整個靜態站。

## 核心資料流
`input → calendar → hexagram → palace/世應 → 納甲 → 六親 → 伏神/飛神 → 六神 → 日月/空破 → 用神 → 動變 → judgement → timing`

## 重要設計
- `data/`：較固定的排盤資料。
- `rules/`：可依流派調整的權重、問事分類與判斷原則。
- `question-categories.json` 與 `judgement-rules.json` 已直接驅動用神及判斷；每份 Chart JSON 會記錄實際規則版本。
- `src/engine/`：純函式引擎；UI 不直接硬寫排盤規則。
- 變爻六親仍以前卦卦宮五行為基準。
- 月建自動值由太陽視黃經求十二個「節」的交界時刻；如採用特定曆書，仍可手動覆寫。
- 日界預設民用午夜；採子初換日者請手動覆寫日柱。

## v1.1.0 強化與驗證
- 集中驗證日期、時間、六爻、日柱、月建、分類與用神輸入。
- 最近紀錄改用安全 DOM API，避免問題文字形成儲存型 XSS。
- 輸入 Schema 與實際 `createChart` 介面同步。
- 新增全部 4^6＝4096 種起卦組合測試，覆蓋 64×64 本卦／變卦。
- CI 測試成功後才允許 GitHub Pages 部署。
- 修正手機結果表格撐寬整頁的橫向溢位。
- 修正起卦後最近紀錄要重新整理才顯示。
- `npm run serve` 改用零依賴 Node 靜態伺服器，Windows 可直接執行。
- 問事分類、判斷權重／門檻及規則版本已接入實際執行路徑。
- `npm test` 包含引擎、整合、64 卦資料及 HTTP smoke test。

## v1.2.0 來源追溯與回測

- `rules/source-catalog.json` 列出來源、支持範圍與限制；`rules/rule-pack.json` 固定規則包版本與來源索引。
- 傳統原典與官方天文資料只用於規則追溯，不宣稱六爻具有科學預測效力。
- 最近 30 卦可逐案補登結果與 1–5 分評價，再匯出 JSON／CSV 作後續統計或訓練候選資料。
- 案例格式為 `liuyao-case-v1`，包含 `chart` 與可為空的 `review`；評分是使用者回饋，不等同專家標註。
- 舊版保留於 GitHub Pages 的 `/v1.0/` 路徑。

## v1.3.0 伏神／飛神

- 若本卦明爻缺少某六親，從所屬八宮的本宮首卦取同爻位納甲作伏神。
- 每個伏神記錄飛神、飛伏五行關係、日月旺衰、本宮來源，並可成為用神候選。
- 排盤、判斷理由、複製摘要與 Chart JSON 均會標示伏神；伏神不等同自動吉凶。

## 下一版可擴充但不破壞架構
墓絕、十二長生、進退神、反吟伏吟、三合局成局條件、不同流派 Rule Pack、案例回測與人工標註。
